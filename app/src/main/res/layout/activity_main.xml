<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/title"
        android:textSize="@dimen/title_text"
        android:paddingBottom="@dimen/title_padding"/>

    <dkapps.throttleseekbar.ThrottleSeekbar
        android:layout_width="wrap_content"
        android:layout_height="@dimen/throttle_length"
        android:layout_gravity="center"
        android:minHeight="@dimen/throttle_radius"
        android:maxHeight="@dimen/throttle_radius"
        custom:backgroundColor="@color/colorPrimary"
        custom:orientation="vertical"/>
</LinearLayout>
